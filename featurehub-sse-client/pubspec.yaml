name: featurehub_sse_client
description: A Dart-IO based client implementation of Server-Sent Events.
version: 1.4.0
publish_to: "none"

environment:
  sdk: '>=2.12.0 <3.10.2'

dependencies:
  collection: ^1.15.0
  http: ^1.5.0
  http_parser: ^4.0.0
  logging: ^1.0.0

dev_dependencies:
  test: ^1.16.5
